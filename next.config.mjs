/** @type {import('next').NextConfig} */
/*
const nextConfig = {
  async redirects() {
    return [
      {
        source: "/:path*",
        has: [
          {
            type: "host",
            value: "bellu.ch",
          },
        ],
        destination: "https://www.bellu.ch/:path*",
        permanent: true,
      },
    ];
  },
};

export default nextConfig;*/
